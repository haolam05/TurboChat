<% room = Room.where(name: "private_#{[current_user.id, user.id].sort.join("_")}").first %>

<%= turbo_frame_tag dom_id(room) do %>
  <%= render 'users/user_container',
        room: room,
        last_message: room.latest_message, 
        user: user %>
<% end %>
<div style="margin-bottom: 10px">
  <%= form_with(model: @room, remote: true, class: "d-flex" ) do |f| %>
    <%= f.text_field :name, class: "form-control", autocomplete: 'off', style: 'margin: 5px;' %>
    <%= f.submit data: { disable_with: false }, class: "btn btn-primary", value: "New Room", style: "margin-bottom: 3px" %>
  <% end %>
</div>
